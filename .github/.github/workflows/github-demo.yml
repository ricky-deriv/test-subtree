name: Workflow running scripts
on: 
  pull_request:

defaults:
  run:
    shell: bash

jobs:
  run-scripts:
    name: "run scripts"
    steps:
    - uses: actions/checkout@v3
    - name: run scripts
      run: |
        bash scriptfile.sh
